import { FormEvent,useState } from 'react';import { api,ApiError } from '@/lib/api';type Order={id:string;status?:string;origin?:string;customerName?:string};type Item={productId:string;quantity:string};const origins=['POS','WHATSAPP','CATALOG','MANUAL'];export default function OrdersPage(){const [order,setOrder]=useState<Order|null>(null);const [orderId,setOrderId]=useState('');const [origin,setOrigin]=useState('POS');const [items,setItems]=useState<Item[]>([{productId:'',quantity:'1'}]);const [customerName,setCustomerName]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function load(e:FormEvent){e.preventDefault();if(!orderId.trim())return;setLoading(true);setError('');try{setOrder(await api<Order>(`/orders/${encodeURIComponent(orderId.trim())}`))}catch(err){setOrder(null);setError(err instanceof ApiError?err.message:'Erro ao consultar pedido')}finally{setLoading(false)}}async function create(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const data=await api<Order>('/orders',{method:'POST',body:JSON.stringify({origin,customerName:customerName||undefined,items:items.filter(i=>i.productId.trim()).map(i=>({productId:i.productId.trim(),quantity:i.quantity}))})});setOrder(data);setOrderId(data.id);setItems([{productId:'',quantity:'1'}])}catch(err){setError(err instanceof ApiError?err.message:'Erro ao criar pedido')}finally{setLoading(false)}}async function cancel(){if(!order?.id)return;setLoading(true);setError('');try{setOrder(await api<Order>(`/orders/${order.id}/cancel`,{method:'POST'}))}catch(err){setError(err instanceof ApiError?err.message:'Erro ao cancelar pedido')}finally{setLoading(false)}}return <section className="page"><header><h1>Pedidos</h1><p>Criação e cancelamento passam pelo backend e pelo contexto da loja.</p></header><form className="card stack" onSubmit={create}><h2>Novo pedido</h2><select className="input" value={origin} onChange={e=>setOrigin(e.target.value)}>{origins.map(o=><option key={o}>{o}</option>)}</select><input className="input" value={customerName} onChange={e=>setCustomerName(e.target.value)} placeholder="Cliente (opcional)"/>{items.map((item,index)=><div className="grid" key={index}><input className="input" value={item.productId} onChange={e=>setItems(prev=>prev.map((x,i)=>i===index?{...x,productId:e.target.value}:x))} placeholder="ID do produto" required/><input className="input" value={item.quantity} onChange={e=>setItems(prev=>prev.map((x,i)=>i===index?{...x,quantity:e.target.value}:x))} placeholder="Quantidade" required/></div>)}<button className="btn secondary" type="button" onClick={()=>setItems(prev=>[...prev,{productId:'',quantity:'1'}])}>Adicionar item</button><button className="btn" disabled={loading}>{loading?'Processando...':'Criar pedido'}</button></form><form className="card stack" onSubmit={load}><h2>Consultar pedido</h2><input className="input" value={orderId} onChange={e=>setOrderId(e.target.value)} placeholder="ID do pedido"/><button className="btn secondary" disabled={loading}>Consultar</button></form>{error&&<div className="card" role="alert">{error}</div>}{order&&<article className="card"><h2>Pedido {order.id}</h2><div className="grid"><div><small>Status</small><h3>{order.status||'—'}</h3></div><div><small>Origem</small><h3>{order.origin||'—'}</h3></div><div><small>Cliente</small><h3>{order.customerName||'—'}</h3></div></div><button className="btn secondary" disabled={loading} onClick={()=>void cancel()}>Cancelar pedido</button></article>}</section>}
