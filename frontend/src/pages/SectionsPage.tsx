import { FormEvent,useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';type Section={id:string;name:string;category?:{name:string};pricingPolicy?:{markupPercent:number}};type Taxonomy={sections?:Section[]};export default function SectionsPage(){const [items,setItems]=useState<Section[]>([]);const [error,setError]=useState('');const [loading,setLoading]=useState(false);const [sectionId,setSectionId]=useState('');const [markupPercent,setMarkupPercent]=useState('');async function load(){setLoading(true);setError('');try{const d=await api<Taxonomy>('/catalog/taxonomy');setItems(d.sections||[])}catch(e){setError(e instanceof ApiError?e.message:'Erro ao carregar seções')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function save(e:FormEvent){e.preventDefault();if(!sectionId||!markupPercent)return;setLoading(true);setError('');try{await api(`/catalog/sections/${encodeURIComponent(sectionId)}/pricing-policy`,{method:'PUT',body:JSON.stringify({markupPercent:Number(markupPercent)})});setMarkupPercent('');await load()}catch(err){setError(err instanceof ApiError?err.message:'Não foi possível atualizar a margem da seção')}finally{setLoading(false)}}return <section className="page"><header><h1>Seções</h1><p>Defina a margem padrão por seção para sugerir o preço de venda automaticamente.</p></header><form className="card stack" onSubmit={save}><h2>Margem padrão da seção</h2><select className="input" value={sectionId} onChange={e=>setSectionId(e.target.value)} required><option value="">Selecione a seção</option>{items.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input className="input" type="number" min="0" step="0.01" value={markupPercent} onChange={e=>setMarkupPercent(e.target.value)} placeholder="Margem em %" required/><button className="btn" disabled={loading}>{loading?'Salvando...':'Salvar margem'}</button></form>{error?<div className="card" role="alert">{error}</div>:loading&&items.length===0?<div className="card">Carregando...</div>:!items.length?<div className="card">Nenhuma seção cadastrada.</div>:<div className="card"><table className="table"><thead><tr><th>Seção</th><th>Categoria</th><th>Margem padrão</th></tr></thead><tbody>{items.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.category?.name||'—'}</td><td>{s.pricingPolicy?.markupPercent==null?'—':`${s.pricingPolicy.markupPercent}%`}</td></tr>)}</tbody></table></div>}</section>}
