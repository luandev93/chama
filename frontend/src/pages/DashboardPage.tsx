import { useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';type Overview={cards:{revenue:number;orders:number;ordersToday:number;averageTicket:number;estimatedProfit:number;estimatedMargin:number;expiringLots:number;stockouts:number;lowStock:number;inventoryValue:number;activePromotions:number};topProducts:{productId:string;name:string;qty:number;revenue:number;profit:number;margin:number}[]};const money=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));export default function DashboardPage(){const [data,setData]=useState<Overview|null>(null);const [error,setError]=useState('');useEffect(()=>{api<Overview>('/dashboard/overview').then(setData).catch(e=>setError(e instanceof ApiError?e.message:'Não foi possível carregar o painel'))},[]);if(error)return <section className="page"><h1>Painel do gestor</h1><div className="card" role="alert">{error}</div></section>;if(!data)return <section className="page"><h1>Painel do gestor</h1><p>Carregando indicadores...</p></section>;const c=data.cards;const metrics=[['Faturamento',money(c.revenue)],['Lucro bruto estimado',money(c.estimatedProfit)],['Margem bruta',`${c.estimatedMargin.toFixed(1)}%`],['Pedidos',String(c.orders)],['Ticket médio',money(c.averageTicket)],['Pedidos hoje',String(c.ordersToday)],['Estoque crítico',String(c.lowStock)],['Rupturas',String(c.stockouts)],['Lotes a vencer',String(c.expiringLots)],['Promoções ativas',String(c.activePromotions)]];return <section className="page"><header><h1>Painel do gestor</h1><p>Visão operacional, financeira e comercial da loja nos últimos 30 dias.</p></header><div className="grid">{metrics.map(([label,value])=><article className="metric" key={label}><small>{label}</small><h2>{value}</h2></article>)}</div><section className="grid"><article className="card"><h2>Capital em estoque</h2><p><strong>{money(c.inventoryValue)}</strong></p><small>Valor de venda estimado do saldo disponível.</small></article><article className="card"><h2>Alertas prioritários</h2><p><strong>{c.lowStock}</strong> itens em nível crítico</p><p><strong>{c.expiringLots}</strong> lotes próximos do vencimento</p><p><strong>{c.stockouts}</strong> produtos sem saldo</p></article></section><article className="card"><h2>Produtos que mais vendem</h2>{data.topProducts.length===0?<p>Ainda não há vendas suficientes para montar o ranking.</p>:<table className="table"><thead><tr><th>Produto</th><th>Qtd.</th><th>Faturamento</th><th>Lucro bruto</th><th>Margem</th></tr></thead><tbody>{data.topProducts.map(p=><tr key={p.productId}><td><strong>{p.name}</strong></td><td>{p.qty}</td><td>{money(p.revenue)}</td><td>{money(p.profit)}</td><td>{p.margin.toFixed(1)}%</td></tr>)}</tbody></table>}</article></section>}
