import { useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';type Section={id:string;name:string;category?:{name:string};pricingPolicy?:{markupPercent:number}};type Taxonomy={sections?:Section[]};export default function SectionsPage(){const [items,setItems]=useState<Section[]>([]);const [error,setError]=useState('');useEffect(()=>{api<Taxonomy>('/catalog/taxonomy').then(d=>setItems(d.sections||[])).catch(e=>setError(e instanceof ApiError?e.message:'Erro ao carregar seções'))},[]);return <section className="page"><h1>Seções</h1>{error?<div className="card" role="alert">{error}</div>:!items.length?<div className="card">Nenhuma seção cadastrada.</div>:<div className="card"><table className="table"><thead><tr><th>Seção</th><th>Categoria</th><th>Margem padrão</th></tr></thead><tbody>{items.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.category?.name||'—'}</td><td>{s.pricingPolicy?.markupPercent==null?'—':`${s.pricingPolicy.markupPercent}%`}</td></tr>)}</tbody></table></div>}</section>}
