import { useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';
type Props={title:string;description:string;endpoint:string;columns:string[]};
export default function CrudPage({title,description,endpoint,columns}:Props){const [rows,setRows]=useState<Record<string,unknown>[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);setError('');try{const data=await api<unknown>(endpoint);setRows(Array.isArray(data)?data:((data as {items?:Record<string,unknown>[]}).items||[]))}catch(e){setError(e instanceof ApiError?e.message:'Erro ao carregar')}finally{setLoading(false)}}useEffect(()=>{void load()},[endpoint]);return <section className="page"><header><h1>{title}</h1><p>{description}</p></header>{loading&&<div className="card">Carregando...</div>}{error&&<div className="card" role="alert">{error}<div style={{marginTop:12}}><button className="btn" onClick={()=>void load()}>Tentar novamente</button></div></div>}{!loading&&!error&&rows.length===0&&<div className="card">Nenhum registro encontrado.</div>}{!loading&&!error&&rows.length>0&&<div className="card"><table className="table"><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={String(row.id||i)}>{columns.map(c=><td key={c}>{String(row[c]??'—')}</td>)}</tr>)}</tbody></table></div>}</section>}
