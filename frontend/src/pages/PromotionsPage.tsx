import { FormEvent,useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';type Product={id:string;name:string;pricing?:{basePrice?:string|number}};type Promotion={id:string;title:string;productName?:string;basePrice?:string|number;promotionPrice?:string|number;percentOff?:string|number;status:string;startsAt:string;endsAt?:string};const money=(v:any)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));export default function PromotionsPage(){const [items,setItems]=useState<Promotion[]>([]);const [products,setProducts]=useState<Product[]>([]);const [productId,setProductId]=useState('');const [title,setTitle]=useState('');const [promotionPrice,setPromotionPrice]=useState('');const [percentOff,setPercentOff]=useState('');const [startsAt,setStartsAt]=useState(new Date().toISOString().slice(0,16));const [endsAt,setEndsAt]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function load(){setLoading(true);setError('');try{const [promos,prods]=await Promise.all([api<Promotion[]>('/promotions'),api<Product[]>('/catalog/products')]);setItems(promos);setProducts(prods)}catch(e){setError(e instanceof ApiError?e.message:'Não foi possível carregar promoções')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function create(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{await api('/promotions',{method:'POST',body:JSON.stringify({productId,title:title||undefined,promotionalPrice:promotionPrice||undefined,percentOff:promotionPrice?undefined:percentOff||undefined,startsAt:new Date(startsAt).toISOString(),endsAt:endsAt?new Date(endsAt).toISOString():undefined,status:'ACTIVE'})});setProductId('');setTitle('');setPromotionPrice('');setPercentOff('');setEndsAt('');await load()}catch(err){setError(err instanceof ApiError?err.message:'Não foi possível criar promoção')}finally{setLoading(false)}}async function status(id:string,status:string){setLoading(true);setError('');try{await api(`/promotions/${id}/status`,{method:'POST',body:JSON.stringify({status})});await load()}catch(err){setError(err instanceof ApiError?err.message:'Não foi possível atualizar promoção')}finally{setLoading(false)}}const selected=products.find(p=>p.id===productId);return <section className="page"><header><h1>Promoções</h1><p>Crie ofertas reais. O catálogo e o PDV passam a calcular automaticamente o preço efetivo ativo.</p></header><form className="card stack" onSubmit={create}><h2>Nova promoção</h2><select className="input" value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Selecione o produto</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select>{selected&&<p>Preço atual: <strong>{money(selected.pricing?.basePrice)}</strong></p>}<input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Nome da oferta (opcional)"/><div className="grid"><input className="input" inputMode="decimal" value={promotionPrice} onChange={e=>{setPromotionPrice(e.target.value);if(e.target.value)setPercentOff('')}} placeholder="Preço promocional"/><input className="input" inputMode="decimal" value={percentOff} disabled={Boolean(promotionPrice)} onChange={e=>setPercentOff(e.target.value)} placeholder="ou desconto (%)"/></div><div className="grid"><input className="input" type="datetime-local" value={startsAt} onChange={e=>setStartsAt(e.target.value)} required/><input className="input" type="datetime-local" value={endsAt} onChange={e=>setEndsAt(e.target.value)}/></div><button className="btn" disabled={loading||!productId||(!promotionPrice&&!percentOff)}>{loading?'Salvando...':'Ativar promoção'}</button></form>{error&&<div className="card" role="alert">{error}</div>}<div className="card"><h2>Promoções cadastradas</h2>{items.length===0?<p>Nenhuma promoção cadastrada.</p>:<table className="table"><thead><tr><th>Produto</th><th>Normal</th><th>Oferta</th><th>Status</th><th>Período</th><th>Ação</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td>{p.productName||p.title}</td><td>{money(p.basePrice)}</td><td>{p.promotionPrice?money(p.promotionPrice):`${p.percentOff}% OFF`}</td><td>{p.status}</td><td>{new Date(p.startsAt).toLocaleString('pt-BR')}{p.endsAt?` → ${new Date(p.endsAt).toLocaleString('pt-BR')}`:''}</td><td>{p.status==='ACTIVE'?<button className="btn secondary" disabled={loading} onClick={()=>void status(p.id,'PAUSED')}>Pausar</button>:<button className="btn secondary" disabled={loading} onClick={()=>void status(p.id,'ACTIVE')}>Ativar</button>}</td></tr>)}</tbody></table>}</div></section>}
