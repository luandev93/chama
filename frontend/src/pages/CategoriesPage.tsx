import { useEffect,useState } from 'react';import { api,ApiError } from '@/lib/api';type Category={id:string;name:string;sections?:unknown[]};type Taxonomy={categories?:Category[]};export default function CategoriesPage(){const [items,setItems]=useState<Category[]>([]);const [error,setError]=useState('');useEffect(()=>{api<Taxonomy>('/catalog/taxonomy').then(d=>setItems(d.categories||[])).catch(e=>setError(e instanceof ApiError?e.message:'Erro ao carregar categorias'))},[]);return <section className="page"><h1>Categorias</h1>{error?<div className="card" role="alert">{error}</div>:!items.length?<div className="card">Nenhuma categoria cadastrada.</div>:<div className="card"><table className="table"><thead><tr><th>Categoria</th><th>Seções</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.sections?.length||0}</td></tr>)}</tbody></table></div>}</section>}
